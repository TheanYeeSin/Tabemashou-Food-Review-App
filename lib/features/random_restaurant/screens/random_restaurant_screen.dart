import "package:flutter/material.dart";

class RandomRestaurantScreen extends StatelessWidget {
  const RandomRestaurantScreen({super.key});

  @override
  Widget build(final BuildContext context) =>
      Scaffold(appBar: AppBar(title: const Text("Random Restaurant")));
}
